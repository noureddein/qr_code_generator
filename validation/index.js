const generate = require("./generate.valid");
const user = require("./user.valid");
const auth = require("./auth.valid");
const qrCodes = require("./qrCodes.valid");

module.exports = {
	generate,
	user,
	auth,
	qrCodes,
};
